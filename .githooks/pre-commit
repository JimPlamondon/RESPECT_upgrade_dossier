#!/usr/bin/env bash
# SPDX-FileCopyrightText: 2026 Jim Plamondon
# SPDX-License-Identifier: Apache-2.0

set -euo pipefail

branch="$(git branch --show-current)"
if [ "${branch}" = "main" ]; then
  printf 'Refusing authored commits on main; use a task branch.\n' >&2
  exit 1
fi

git diff --cached --check
